{
  "name": "sistema-buste-paga-badante",
  "version": "1.0.0",
  "description": "Applicazione gestionale per calcolo stipendio badante, contributi INPS e buste paga",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
